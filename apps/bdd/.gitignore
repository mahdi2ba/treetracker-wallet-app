node_modules
#test-artifacts/
logs/
.env


test-artifacts/test-videos/.tmp/
test-artifacts/test-videos/**.log

# Per-scenario videos copied into the published report at run time. They ship to
# GitHub Pages from the working tree (gh-pages ignores .gitignore), so keep the
# binaries out of the main branch.
test-artifacts/reports/**/cucumber-html/videos/
